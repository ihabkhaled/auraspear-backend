{
  "buildCommand": "npx prisma migrate resolve --rolled-back 20260314060000_add_connector_last_sync_at && npx prisma generate && npx prisma migrate deploy && npm run build && npx prisma db seed",
  "installCommand": "npm install"
}
